# The plugin resolves harness packages as peers from the profile's own
# installation; auto-installing them here would give it a second copy of a
# service identity compared by reference.
auto-install-peers=false
